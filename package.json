{
  "name": "riffle",
  "workspaces": [
    "riffle-client",
    "riffle-server"
  ],
  "scripts": {
    "start": "npm start -w riffle-server",
    "build": "export PRODUCTION=true; sh ./ci-cd/build.sh",
    "deploy": "git push -f heroku master"
  }
}
